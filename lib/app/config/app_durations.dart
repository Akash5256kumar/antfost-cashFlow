class AppDurations {
  const AppDurations._();

  static const Duration splashTransition = Duration(milliseconds: 500);
  static const Duration pageScroll = Duration(milliseconds: 350);
  static const Duration indicatorAnimation = Duration(milliseconds: 300);
}
